This directory keeps deprecated inputs for reference only.

- `sm_families/`: old standalone family JSONs (`pion_like.json`, etc.).  
  Current code uses `data/raw/sm_universe.json` instead.

These files are not used by the pipeline; keep them only as historical reference.
